#include <iostream>
using namespace std;

int main() {
	double sum = 0, x, a;
	int k, n = 1;
	cin >> k >> x;
	for (int i = 1; i <= k; ++i) {
		n *= i;
	}
	a = -1 * x * x / n;
	for (int i = 1; i < k; ++i) {
		sum += a;
		a *= -1 * x * x;
		cout << sum << endl;
	}

}
