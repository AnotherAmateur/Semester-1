#include <iostream>
using namespace std;

int main() {
	double A, a1 = 1, a2 = 0.5;
	int n;
		cin >> n;
		if (n == 0 ) return 0;
		if (n > 0) cout << a1 << endl;
		else cout << "incorrect n";
		if (n > 1) cout << a2 << endl;
		for (int i = 2; i < n; ++i) {
			A = sin(a1) + (a2 / 2);
			a1 = a2; a2 = A;
			cout << fixed << A << endl;
		}
}
